import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    htmlFontSize: 16,
    fontFamily: `"IRANSansXFaNum","sansserif"`,
    fontSize: 25,
  },
});
export { theme };
